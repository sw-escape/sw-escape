import 'package:flutter/material.dart';

class CoreLiberalArts extends StatelessWidget {
  const CoreLiberalArts({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
